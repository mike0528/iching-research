# 專案協作規則

## 工作方式

- 使用繁體中文溝通，程式碼、變數名稱與技術名詞保留原文。
- 非簡單任務先檢查專案結構、現有程式碼、Git 狀態與可用的測試指令。
- 先提出精簡的實作計畫，再以小步驟修改；避免沒有必要的重構與過度設計。
- 優先遵循專案既有的架構、命名、格式化與相依套件，不任意引入新套件。
- 修改後執行適用的測試、lint、typecheck 或建置指令。
- 回報修改的檔案、驗證指令、結果，以及尚未解決的問題。

## Git 規則

- 開始修改前先確認 `git status`。
- 不自動執行 `git commit`、`git push`、`git reset --hard`、force push 或刪除分支。
- 完成工作後先提供 diff 與建議的 commit 說明，等使用者確認後再提交。
- 不覆蓋使用者尚未提交的修改；遇到不明來源的變更先詢問。

## 安全邊界

- 不讀取、顯示或提交密鑰、Token、`.env`、私密設定與個人資料。
- `rm -rf`、大量刪除、部署、付款、正式資料提交、資料庫破壞性操作都必須先取得明確確認。
- 不因測試方便而降低安全設定或繞過權限。
- 使用瀏覽器時，只有在使用者明確要求後才操作；不代替使用者輸入密碼。付款、刪除、發佈與正式提交前必須人工確認。

## 工作區導入計劃提醒

- 開始非 L0 工作時，若 `docs/導入計劃書.md` 的預定檢視日或任務數條件已到，先提醒使用者檢視下一階段。
- 提醒不等於自動導入；任何後續 Skill、Extension、套件或設定變更，都要先取得使用者確認。
- Pi 沒有在工作區關閉時主動推播的背景排程；提醒會在下次使用工作區或執行 `/work-start` 時出現。

## 完成條件

一項工作只有在以下條件都具備時才算完成：

1. 實作符合使用者的驗收條件。
2. 相關測試或檢查已執行，或清楚說明為何無法執行。
3. 變更範圍維持最小且沒有意外修改。
4. 使用者知道下一步需要做什麼。
